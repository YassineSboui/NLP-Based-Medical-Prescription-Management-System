"""Frontend building blocks.

Split out of the single 1,213-line module the previous frontend was, roughly
830 lines of which were CSS carried inline in two blocks that had drifted apart
from each other. The stylesheet now lives in ``frontend/assets/app.css`` and is
loaded once.
"""
